# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Node (in case we add a translator script later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local-only Claude Code config
.claude/settings.local.json

# Staging area — source files awaiting translation are NOT published.
# Only the translated output under content/ gets committed.
books_for_processing/

# Secrets — never commit any .env file from any subdirectory
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
