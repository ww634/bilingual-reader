# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Node (in case we add a translator script later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local-only Claude Code config
.claude/settings.local.json

# Secrets — never commit any .env file from any subdirectory
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local
